import * as React from 'react';
declare type NVButtonProps = {
    variant?: string | 'text';
    defaultValue?: number;
    children: React.ReactNode;
};
export default function NVButton(props: NVButtonProps | any): JSX.Element;
export {};
